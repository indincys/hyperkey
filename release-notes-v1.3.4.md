## 按住 Hyper，连续按键可正常往返

- 修复长按 Caps Lock（Hyper）约 400 毫秒后被错误判定为已经松开的问题；现在长按期间可连续按同一个应用字母。
- 默认「按一次打开，再按一次返回」行为会记住打开前的应用，第二下明确切回，不再依赖目标应用是否接受系统的隐藏请求。
- Chrome 等拒绝 `hide()` 的应用现在也能正常返回；不再把失败的隐藏请求误记成成功。

## 可靠性

- 看门狗改为读取键盘原始 HID 状态，真实松键仍会立即释放全部 Hyper 修饰键；原始状态不可用时使用有上限的安全回退。
- 返回后会核验系统真实前台应用，首次激活未生效时通过工作区接口自动补试。
- 完整测试套件 349 项通过，发布包继续使用原固定证书签名，升级无需重新授予辅助功能权限。
