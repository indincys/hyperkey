## 修复：切到「文件」等分类会直接退出

1.4.0 里，列表中只要有图片网格，切换筛选页就可能让应用崩溃。

原因是列表、分组标题和排版三者是三个独立的 `@Published` 属性。它们必须互相吻合——排版按下标索引到具体条目——但分开发布意味着 SwiftUI 可以在其中两次写入之间跑一次渲染：于是一张横跨第 0~5 行的图片网格，被画在了一个刚刚只剩三个文件的列表上，下标越界。

现在三者是同一个值，不可能被观察到只写了一半；视图侧另外再做一次夹取。附带的回归测试会把真正的面板放进宿主视图里切页，在旧代码上会稳定复现这次崩溃。

## 面板响应

面板整体不够跟手、快速划过列表有拖影，测下来是四件事：

- **筛选药丸原来占了整个面板 85% 的渲染开销。** 那一行用 `ViewThatFits` 在四种密度里挑一种，也就是每次布局都要摆 28 个药丸再扔掉——而指针每划过一行，面板就布局一次。实测每行 10.6ms，去掉头部只剩 1.5ms。`.equatable()` 挡不住：它挡的是 body 重算，`ViewThatFits` 重算的是布局。改成直接测量文字宽度（结果带缓存）算出该用哪种密度，并把算好的宽度直接交给每个药丸。每行 10.6ms → 3.1ms，且不随历史条数增长。
- **行高亮原来有 0.1s 淡入。** 这是选中行还是强调色填充时留下的；现在悬停即选中，于是快速划过会拖一串半亮的行。改成瞬时。
- **预览卡片原来会动画跟随指针。** 一张永远慢指针几帧的卡片，看上去就是面板在卡。改成直接就位。
- **切换筛选页原来整列表做进出动画。** 二十行往上滑出、三行往下滑入，重叠在一起就是拖影。现在只有增删条目才有动画，整列表替换不做。

另外，指针每划过一行原来会发布三次状态变更（其中两次内容没变），搜索框每次刷新都要重建一遍占位文字和颜色。都改成只在真变了的时候写。

## 兼容性

- 只改渲染与状态发布方式，功能、快捷键、数据格式一律未动。
- 签名身份不变，从 1.4.0 应用内更新即可。
