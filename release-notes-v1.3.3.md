## 按住查看，松开返回

- 新增真正的 Hold-to-Peek 应用快捷键模式：按住 Hyper + 字母时切到对应应用，松开字母或 Hyper 后自动隐藏目标并回到原应用。
- 冷启动应用即使在松键后才完成启动，也会被正确收回，不会延迟抢走焦点。
- 按住 Hyper 连续换按不同应用字母时，会稳定保留最初的返回目标，不受松键顺序影响。

## 设置与兼容性

- 「通用」中的应用快捷键行为现在提供：按住查看、双按隐藏、循环窗口和只打开四种选择。
- 配置文件可使用 `"repeatPress": "peek"` 启用新模式。
- 原有 `hide`、`cycle`、`none` 以及旧版 `toggleHideIfFrontmost` 配置继续兼容，升级不会擅自改变现有手势。
