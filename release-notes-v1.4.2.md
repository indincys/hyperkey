## 快捷键和应用切换更可靠

- 修复事件监听被 macOS 暂停后可能看似仍在运行、实际不再响应的问题；现在会验证监听健康状态，并在原地恢复失败时限次重建。
- 修复暂停 Hyper 时 Caps Lock 映射出的 F19 可能泄漏给前台应用，以及在按住组合键途中暂停可能留下修饰键锁定的问题。
- 应用正在冷启动时会合并重复按键，避免慢应用连续抢回焦点；快速连续切换、临时查看（peek）和隐藏回退也不会再被上一轮延迟任务打断。
- 临时查看结束改用与普通隐藏相同的多级回退路径；拔出键盘或蓝牙断连造成的异常长按也有明确的安全上限。
- HID 映射的应用、重连和退出恢复改为严格串行；退出时即使恰逢键盘重连，也不会把 Caps Lock 留在 Hyper 的临时映射上。

## 剪贴板面板更快、更稳

- 面板在历史加载后预热，首次呼出不再同时承担窗口和 SwiftUI 树的初始化成本。
- 轮询频率会随面板可见性和空闲时间自适应：面板打开时更快捕获菜单/右键复制，长期空闲时减少无意义唤醒。
- 修复中文等输入法正在组词时，回车、方向键或重复聚焦可能吞掉候选和已输入拼音的问题。
- 图片网格支持跨分块拖框选择，长图片列表分段后也能连续选中；网格高度现在按滚动区域的真实宽度计算，不再留下空白或让选择坐标错位。
- 修复缩略图缓存因无关历史变动频繁失效、不同显示尺寸互相覆盖、缩略图刚落盘时被长期负缓存，以及本地文件被误标为“网络卷”等问题。
- 增强“减少透明度”“增强对比度”和 VoiceOver 表现；高对比度下文字与边框更清晰，遮罩内容不会继续被辅助功能光标访问。

## 大历史库的性能与一致性

- 搜索索引的过滤槽改为常数时间查找和删除，并对墓碑、映射和 ID 集合做完整内存预算；索引内部不一致时拒绝把损坏分片写成合法空分片。
- 搜索过程复用查询和记录的规范化结果，批量删除一次处理整组索引变更，避免大历史库里重复扫描、搬移和整表重建。
- 历史插入、去重、置顶、批量删除和恢复路径减少全量排序与线性查找；批量删除使用单一可恢复墓碑并在返回前固化索引。
- 捕获预览只扫描真正会显示的文本前缀；摘要格式化、字节格式化、摘要派生和粘贴格式转换均改为复用结果，降低滚动、搜索和批量粘贴的主线程开销。

## 兼容性

- 现有历史库与快捷键配置保持兼容，签名身份不变，可从 1.4.1 直接应用内更新。
