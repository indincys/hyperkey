## 剪贴板稳定性修复

- 修复 Mac 从睡眠或锁屏恢复后漏记最后一次复制的问题：恢复时先消费停用期间的最新剪贴板事务，再重建轮询基线。
- SwiftPM / QA 调试实例改用进程隔离的临时保险库，不再误碰生产历史库、触发安全 Broker 拒绝并让剪贴板落入整段会话只读。
- 只读诊断日志现在会记录明确原因，后续若遇到密钥、认证或布局故障，不再只有笼统的“捕获失败”。

## 钥匙串与升级

- 固定签名的 `HyperKeyBroker` 二进制、SHA-256 和 CDHash 保持不变；已经完成 v1.3.1 一次性迁移的用户无需再次输入钥匙串访问密码。
- 普通密钥读取继续使用禁止交互模式，应用重启和后续主程序更新不会主动弹出钥匙串密码框。
- v1.3.1 可直接通过应用内更新升级到本版本，签名身份不变，不需要重新授予“辅助功能”权限。
