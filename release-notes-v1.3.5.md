## 应用切换

- 修复运行中的应用只接管菜单栏、窗口却仍留在后台的问题；微信等应用现在会可靠地把现有窗口带到前台。
- 应用最后一个可见窗口已经关闭时，快捷键会按 macOS 标准“重新打开”语义恢复窗口，不再停在只有菜单栏的空前台状态。

## 可靠性

- 普通切换、再次按键返回和“按住查看”现在共用同一套窗口激活路径。
- 冷启动仍保留原来的 LaunchServices 启动事件，避免为了修复运行中应用而改变首次启动行为。
- 新增运行中应用重开事件与冷启动事件的回归测试。
