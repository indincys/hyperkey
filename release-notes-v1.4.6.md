## 修复：图片网格里 ⌘点击 连续粘贴失效

**按住 ⌘ 点击图片不再有任何反应**——而 `⌘点击` 正是连续粘贴（粘完不关面板，接着点下一张）的手势，所以这条路径在图片上完全断了；文字行、文件行都还正常。

原因在图片网格上那层「框选」视图。`⌘拖拽` 框选多张图，靠的是一个盖在所有缩略图之上的透明 `NSView`；它必须在**鼠标按下的那一刻**就接手事件，否则等发现是拖拽就已经晚了——而一旦接手，AppKit 就会把整个鼠标序列都发给它，底下 SwiftUI 缩略图自己的点击手势再也不会触发。文件行的拖拽桥接（`MultiFileDragNSView`）一直处理得很好：它在**松手时如果发现没有拖动**，就把这次点击交还回去当作激活。框选视图少了这一步。

两个后果，现在都修好了：

- `⌘点击` 图片被框选视图吞掉，什么也没发生。现在松手时若没拖动，就激活指针下那张图——和第 1 张、第 20 张无关，按指针实际落点算。
- 更糟的是，按下时它会**清空多选**。所以正在多选时随手 `⌘点击` 一下，选好的就没了。现在清空**推迟到真正开始拖拽的那一刻**，点击不再动选择。

顺带加了 3 点的拖动阈值（与文件行桥接一致）：点击时手抖一两个像素仍算点击，不会变成一条一像素宽的框选。按下在缩略图之间的缝隙里、或落在一行末尾的空位上，则什么也不激活——不猜。

## 兼容性

- 只改鼠标事件如何交还，历史库、快捷键、筛选、队列一律未动。
- 签名身份不变，从 1.4.5 应用内更新即可。
