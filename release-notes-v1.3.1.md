## 紧急修复：恢复剪贴板捕获与历史

- 修复 v1.3.0 将旧版 `HYPERVLT v1` 历史误判为异常、进入只读恢复，导致复制内容无法进入历史的问题。
- 增加经过认证的 v1 → v2 影子迁移：复用原密钥、逐文件验证、原子替换；认证失败时原目录逐字节不变。
- 引入固定签名的 `HyperKeyBroker`。剪贴板密钥与信任状态只授权给 CDHash 固定的 Broker，主程序后续更新不再反复触发钥匙串密码框。
- 新建本地签名身份 `Hyper Local Secure 2026`，旧的泄露签名私钥不再用于发布；P12/PFX 已加入版本库忽略规则。
- 发布门禁新增旧库迁移、错误密钥不改盘、重启后继续捕获、5000 条加密读写性能、固定 Broker SHA/CDHash 与深度签名验证。

### 一次性手动更新说明

本版本更换了本地签名证书。旧版的安全更新器会拒绝不同证书，因此 **v1.3.1 需要从本发布页下载 ZIP，退出 Hyper 后手动替换 `/Applications/Hyper.app` 一次**。这不是 Apple Developer / 公证版本。

第一次启动时，macOS 可能只弹出一次旧剪贴板密钥迁移授权；请选择“始终允许”。迁移完成后，普通重启与后续主程序更新不应再弹出该密码框。
