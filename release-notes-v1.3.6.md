## 应用切换

- 默认应用快捷键改为与 Raycast 一致的 Toggle Visibility：目标不在前台时显示并激活，已经在前台时隐藏整个应用。
- 成功隐藏后由 macOS 恢复最近使用的应用，不再强制跳回最初打开目标时所在的应用。
- 清理已经确认的临时返回目标，避免用户通过 Dock、Command-Tab 等方式切换后返回到过期应用。

## 兼容性

- Chrome 等拒绝系统隐藏请求的应用仍会自动切回最近确认的前台应用。
- 保留快速连按时的异步兜底，应用切换通知尚未到达也能稳定显示/隐藏。
