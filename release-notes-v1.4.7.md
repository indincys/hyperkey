## 预览更跟手，也不再时快时慢

三处原因，都改掉了：

**1. 方向性保持带来的不对称。** 上一版为了让指针能横穿列表够到另一侧的预览卡片，加的规则是「只要指针在朝卡片方向移动，卡片就保持当前这张」。代价是：同一个动作，朝卡片方向走会保持最多 0.25 秒，朝反方向走却立刻跟随——所以同一段横移，一个方向快、一个方向慢。

现在换成**对称的“停稳才跟随”**：卡片跟着指针**停下来的那一行**走，不管卡片在哪一侧、指针往哪个方向去。

- 指针连续划过若干行时，卡片不动——每进入一行都会重新计时，所以横穿多远都不会中途改目标，原来要够到卡片的问题照样解决。
- 指针停在某一行上，约 **90ms** 后卡片跟过去——短到读起来就是立即。
- 高亮仍然是**立刻**跟着指针走（它必须在眼睛到达之前就亮），只有卡片等。
- 走到卡片上，待处理的切换会被取消，卡片保持你够过去的那张。

这同时让扫过图片列表变快了：只是在某一行的预览不再触发任何解码。

**2. 图片预览不再等大图。** 之前卡片一出现就去读原始内容、解码一张更大的图，再拿它作画——指针停下到画面出现之间隔了一次磁盘读。现在卡片**直接用行里已经解码好的缩略图**（网格里几乎总是已经有了，就在内存里），卡片出现的那一帧图片就在。只有**放大到 1.25 倍以上**、那张小图真的不够看了，才去读原始内容换高清版——卡片的显示倍率一变化就会请求一次，模型侧会去重，不会重复读。浏览是瞬时的，细看仍然清晰。

**3. 文字行和图片行不再是两种东西。** 一列图片都是带边框的彩色方块，文字行却只是浮在面板底色上的一行字——夹在中间读起来像是**空隙**而不是一个条目，最值得读的那几行反而最容易漏掉。

现在文字、代码、链接、文件行也坐在**和缩略图同一块底板**上，边框同理。图片行和颜色行本身已经是一块色块，保持不加底板。深浅两副面孔都调过。

## 兼容性

- 只改悬停时机与行的画法，历史库、快捷键、筛选、队列一律未动。
- 签名身份不变，从 1.4.6 应用内更新即可。
