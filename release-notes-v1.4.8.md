## 列表里能读完了，预览也不再白开

**1. 文本条目按正文长度显示 1 / 2 / 3 行。** 以前每行固定一行，只有选中的那条展开；结果是不逐条悬停就不知道里面是什么。现在行自己按内容长高——一句话就是一行，一句话排到两行就显示两行，**超过三行才截断**。列表本身就是可读的，↑↓ 走一列段落也不用再靠预览。

行数不是按字数估的，是按行宽**真的排了一遍**（CoreText），所以中英混排、整段中文、一长串没有空格的字符，显示的行数和实际折行完全对得上。

**2. 超过三行才开预览卡片。** 这是同一件事的另一半：三行以内的文本，行里已经写完了，悬停不再弹出一块窗口把同样那几行再说一遍。**被截断的才弹**——那正是需要看全文的那些。

图片、链接、文件、颜色**照旧**：缩略图不是图片本身，行首那截路径也不是整个链接，卡片还是它们完整的样子。

行的截断判断和卡片的弹出判断是**同一个函数、同一个宽度**算出来的，所以不会出现「行里截掉了、卡片却不开」这种自相矛盾的情况。

## 兼容性

- 只改行的画法与卡片的弹出条件，历史库、快捷键、筛选、队列一律未动。
- 签名身份不变，从 1.4.7 应用内更新即可。
