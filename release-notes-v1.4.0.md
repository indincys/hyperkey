## 剪贴板面板重做

按高保真原型一比一重构了面板的视觉与交互。功能一件没少，能看见的东西多了近一倍。

- **每一行按类型排版**：文本是 `Aa`，代码 / JSON / 路径是 `❯` 加等宽绿字，链接是首字母图标 + 域名标签（正文改显示域名之后的路径，不再和标签重复），文件是带扩展名的彩色标牌（PSD 蓝、PDF 红、ZIP 琥珀），颜色就是那块颜色本身。
- **行压成一行**：「来源 · 多久以前」不再逐行重复，搬进悬停预览卡片——只在你正在看的那一条上说一次。同样高度装得下差不多两倍的条目。选中的长文本会展开成三行。
- **连续图片折叠成网格**：三张以上挨在一起的图片排成 3 列缩略图。`⌘` 拖拽可以像在访达里一样框选多张，选中的按粘贴顺序编号；`↑↓` 在网格里按行移动，`←→` 按列移动。
- **预览变成卡片**：悬停时出现在列表旁边，宽 300pt，高度按内容裁——图片按自己的比例，文本按自己的长度——并对齐到指针所在的那一行。底部一行写清类型、尺寸、来源、时间和 `⌘C`。
- **深浅两副面孔**：面板头部的 ☾ / ☀ 随时切换，默认跟随系统，选过之后记在配置里（设置 → 剪贴板 → 面板 → 外观，两处是同一个设置）。
- **头部并成一行**：搜索框右侧四个小按钮——切换外观、高级语法、保存筛选、`?` 快捷键速查表。原来占掉一整条的「高级语法 / 保存筛选」文字行没有了。
- **筛选药丸换了个方向**：选中的是实心，其余是描边——七个实心胶囊等于七个都在喊。
- **底部常驻提示条移除**：它在任何列表下都说同样三件事。真正要说的「刚才那下成功了」，现在由粘贴完浮出来的吐司在它为真的那一刻说，且只在面板不关的操作里出现。多选状态改为头部的「已选 N · ↩ 合并粘贴」。快捷键全表仍在 `?`。
- 面板默认更高（标准 400×740），配合更密的行显示更多历史。

## 修复

- 搜索框自带的放大镜图标和头部的那个重叠在一起，把「搜索剪贴板历史」几个字压出一团花；现在只留头部那一个。
- 图片缩略图按比例填充时会溢出所在单元格、盖住下面几行——网格里尤其明显。

## 兼容性

- 拖入拖出、右键菜单、收藏排序、批量队列、撤销删除、VoiceOver 标签与操作全部保持原样。行让出的来源与时间信息只是视觉上的省略，朗读内容一个字没减。
- 签名身份不变，从 1.3.x 应用内更新即可，不需要重新授予「辅助功能」权限。
