## 面板布局：头部不再占掉一整块

头部的四个小按钮（外观、高级语法、保存筛选、`?`）本该排成一行，实际却竖着排成了一列，把顶部约四个按钮的高度都吃掉了，看起来就是搜索框占了好大一块。

原因是那四个按钮是一个自定义视图的 `body`，里面写的是 `Group`。`Group` 在自定义视图的 `body` 里不会摊开进外层的 `HStack`，而是自己排成一列。现在改成一个明确的 `HStack`，四个按钮回到同一行，头部高度从约四行降到一行。搜索行的上下内边距也各收了一点。

## 列表更长

三档高度都调高了：

| | 之前 | 现在 |
|---|---|---|
| 紧凑 | 360×600 | 360×660 |
| 标准 | 400×740 | 400×800 |
| 宽大 | 480×860 | 480×920 |

头部省下来的高度留给了列表。

## 图片预览更大、更清楚

- 预览卡片宽度 300pt → **460pt**，最窄可压到的宽度 240pt → 360pt，图片预览区面积增加一倍以上。卡片最高 360pt → 560pt。
- 文字预览的每行字数改按卡片实际宽度估算，卡片在窄屏上被压窄时不会再少算行数、把最后一行裁掉。
- 卡片里的图片改用**原始内容**解码，不再拿 720px 的缩略图放大——卡片有 920 个设备像素宽，720px 的图放进去是模糊的。原始内容本来就已经存盘（为了粘贴），所以画质上去了，**磁盘一个字节都没多花**；解码出来的位图仍然只由那一个预览缓存按 32MB 上限管着。
- 网格缩略图、行内小图的行为一个字没变，仍然解 720px。悬停到一张图时先显示已经有的缩略图，大图解好再换上，所以不会为了清楚多等一拍空白。

## 默认在鼠标右侧打开

- 新增打开位置「**鼠标右侧**」，并设为默认：面板贴在指针右边，顶边与指针齐平，指针不在任何一行上。
- 右边放不下时自动翻到左边，不会挪到指针底下——落在某一行上会让那一行在第一帧就被悬停，这正是面板的 `hoverArmed` 要防的事。
- 原有「屏幕中央」「鼠标正下方（原「鼠标所在位置」）」「屏幕底部居中」都还在，设置里可以切。
- 顺带修掉一处旧问题：`.center` 会把面板上移屏幕高度的 8%，却没检查结果，在只比面板高一点的屏幕上会把最后几行挂到屏幕外。现在四种位置最后都过同一道夹取，指针模式下还会断言面板绝不包含指针。

## 图片预览可以就地放大

指针落在预览卡片里的图片上，**滚滚轮或双指开合即可放大**——而且放的是**指针底下那一块**：想把哪处细节看清，指哪放大哪，不必先整体放大再去寻找。放大时左上角显示当前倍数。

- 指针移出图片、或换到下一个条目，自动回到适应窗口大小。图片内部的轻微移动不会复位，只有真的移出图片边界才会。
- 双击图片即可复位，方便连续看几处细节。
- **放大后可以拖动查看**：按住左键拖动即可按当前倍数移动到图片的其他位置，倍数不变，光标准备好时变成手形（左上角同时写着「倍数 · 拖动查看」）。拖到边缘会停住，不会把空白玻璃拖进画面中间。
- 放大范围 100%～800%；放大后图片始终铺满卡片，不会把空白玻璃拖到画面中间。
- 只影响预览卡片，网格里的小图仍然是缩略图。

### 缩放灵敏度调到原来的一半

上一版一格滚轮就从 100% 跳到 221%，三格就顶到头，根本没有可瞄准的余地。现在：

- 滚轮按**行数**计、触摸板按**点数**计，两者不再共用一个常数（原来正是共用常数，导致其中一个必然不可用）。实测一格滚轮与触摸板相同力度的一次滑动，缩放幅度基本一致（约 1.42 倍）。
- 单次事件的缩放幅度上限 1.5 倍：一次很猛的滑动不会一步到底。
- `ClipZoomableImageView.zoomFactor(scrollingDeltaY:precise:)` 被单独抽出来，灵敏度本身有了断言，不再靠手感回归。

### 卡片在列表另一侧时够得着

有时预览卡片会展开在列表**左边**——屏幕右侧放不下时就会这样。如果这时指针停在最右边那张小图上，想去看卡片就得横穿整行，而原先指针每划过一个条目卡片就跟着换，等走到卡片时它已经在显示别的图了，等于够不着。

现在卡片会**保持住当前这张**：只要指针还在朝卡片的方向移动，卡片就不换；指针停下来（停在哪一行就切到哪一行），或掉头离开，卡片立刻恢复跟随。判定只看**朝卡片那一个方向**——垂直移动、朝远离卡片的方向移动都是普通悬停，照旧立即切换。卡片在哪一侧由控制器在摆放时告知列表，左右两个方向都适用。

## 兼容性

- 已存在的历史库、快捷键、筛选、队列一律未动。
- `panelPosition` 从未写过的配置会按新的默认值（鼠标右侧）打开；显式选过「屏幕中央」的仍然居中。
- 签名身份不变，从 1.4.2 应用内更新即可。
